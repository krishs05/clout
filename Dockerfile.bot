FROM node:20-alpine

RUN apk add --no-cache openssl

WORKDIR /app

COPY package.json package-lock.json* ./
COPY packages packages
COPY apps apps
COPY turbo.json ./

RUN npm ci && npm run db:generate && npm run build

ENV NODE_ENV=production
CMD ["npm", "run", "start", "--workspace=@clout/bot"]
